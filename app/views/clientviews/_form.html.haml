= form_for @client do |f|
  - if @client.errors.any?
    .card.mx-4
      .card-body
        %div{style: 'color: red'}
          %h2
            =t('application.can_not_be_saved')#Falta traduccion
          %ul
            - @client.errors.each do |error|
              %li= error.full_message

  .form-group.text-center.mx-4.mb-5
    = f.label :client_state, class: "form-label required"
    .div.w-25.mx-auto 
      = f.select(:client_state, options_for_select(client_states_helper, @client.client_state), {prompt: 'select'}, class: "form-control")

  .row.mt-4
    .offset-3.col-3.text-center
      = link_to 'Back', clientviews_path, class: "btn btn-secondary btn-block"
    .col-3.text-center
      = button_tag 'Save', class: "btn btn-outline-success btn-block"
